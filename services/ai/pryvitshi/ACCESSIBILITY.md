# ACCESSIBILITY.md — доступність інтерактивної книги

## 1. Мета

Книга має бути зручною для дітей, учителів і батьків на різних пристроях. Доступність — базова вимога для освітнього продукту.

## 2. Поточний стан — що вже зроблено

| Пункт | Статус |
|---|---|
| Zoom не заблоковано | ✅ |
| Skip-link | ✅ |
| `aria-expanded` / `aria-controls` на меню | ✅ |
| Escape закриває меню | ✅ |
| `aria-live` через окремий `#a11y-announcer` | ✅ |
| `prefers-reduced-motion` — анімації | ✅ |
| `prefers-reduced-motion` — затримки повідомлень | ✅ |
| `prefers-reduced-motion` — анімація переходу між розділами | ✅ |
| `prefers-reduced-motion` — scrollIntoView в активностях | ✅ |
| Змістовні alt для зображень | ✅ |
| `audioDescription` для аудіо | ✅ |
| Progressbar ARIA для активності-класифікатора | ✅ |
| `role="status"` на результаті активності | ✅ |

## 3. Що залишилось

- **Focus trap** для відкритого мобільного меню — Tab не циклить всередині меню
- **Транскрипт пісні** — `audioDescription` є, але повного тексту пісні немає
- Кнопка "Показати весь розділ одразу" — для screen reader і нетерплячих читачів

## 4. Модель аудіо

Поточний формат у `bookSections.js`:

```js
audio: "AI_SONG.mp3",
audioDescription: "Пісня про штучний інтелект, створена ШІ-інструментом"
```

`audioDescription` рендериться як підпис під плеєром і передається в `aria-label` аудіо-елементу.

Якщо в майбутньому з'явиться транскрипт:

```js
audio: "AI_SONG.mp3",
audioDescription: "Пісня про штучний інтелект, створена ШІ-інструментом",
audioTranscript: "Текст пісні..."
```

Рендерер треба буде оновити для підтримки `audioTranscript`.

## 5. Модель зображень

```js
image: "005.webp",
alt: "Конкретний опис зображення для screen reader"
```

Якщо зображення декоративне: `alt: ""`. Усі змістові зображення мають унікальний alt.

## 6. Активності — accessibility

Класифікатор (`activity-classifier.js`):
- Прогрес-бар: `role="progressbar"`, `aria-valuemin/max/now`
- Кнопки карток: `aria-label` з назвою картки і варіантом відповіді
- Результат: `role="status"`, `aria-live="polite"`
- ScrollIntoView поважає `prefers-reduced-motion`

## 7. Клавіатурна навігація

Користувач може:
- перейти до основного контенту через skip-link
- відкрити/закрити меню Enter/Space/Escape
- перейти по пунктах меню Tab
- відповідати на квізи клавіатурою
- натискати "Назад" і "Далі" клавіатурою
- бачити focus outline на всіх активних елементах

## 8. Live region

`#a11y-announcer` — єдиний `aria-live="polite"` регіон. При переході між розділами оголошує назву нового розділу. `chatWindow` не має `aria-live`, щоб уникнути флуду при стаггерованій появі повідомлень.

## 9. Accessibility checklist

- [x] Zoom не заблоковано
- [x] Є skip-link
- [x] Усі кнопки мають зрозумілі назви
- [x] Мобільне меню має `aria-expanded`
- [x] Escape закриває меню
- [x] Видимий focus є на всіх кнопках
- [x] Усі зображення мають змістовні alt
- [x] Аудіо має `audioDescription`
- [x] `prefers-reduced-motion` для анімацій і затримок
- [x] Квізи доступні з клавіатури
- [ ] Focus trap для меню
- [ ] Транскрипт аудіо
- [ ] Кнопка "Показати весь розділ"
